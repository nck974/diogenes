import { GenerateSelectBoxOptionsPipe } from './generate-select-box-options.pipe';

describe('GenerateSelectBoxOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateSelectBoxOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
